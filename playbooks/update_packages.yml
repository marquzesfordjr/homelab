---
- name: Update all packages
  hosts: lxc:proxmox:raspberry:control
  become: true
  tasks:
    - name: Update apt cache and upgrade packages
      apt:
        update_cache: yes
        upgrade: yes

    - name: Remove unused packages
      apt:
        autoremove: yes
